<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!--
            The wall display renders a fortnight of day columns, a rail per day
            and every tab in one response, and the suite runs in a single
            process. 128M is not enough for that and the failure is an opaque
            "premature end of PHP process" rather than a test failure.
        -->
        <ini name="memory_limit" value="512M"/>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_URL" value="http://localhost"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="FAMILYHUB_DISPLAY_TOKEN" value="test-display-token"/>
        <env name="FAMILYHUB_HOUSEHOLD_NAME" value="Test Household"/>
        <env name="FAMILYHUB_SEED_USERS" value="Ada|ada@example.test|secret-password"/>
        <env name="FAMILYHUB_SEED_MEMBERS" value="Ada|#2563eb|adult|;Bea|#16a34a|child|4321"/>
        <env name="FAMILYHUB_IDLE_MINUTES" value="10"/>
        <!-- Empty by default so the suite does not depend on the developer's
             .env; InboundAddressTest sets it explicitly. -->
        <env name="FAMILYHUB_INBOUND_ADDRESS" value=""/>
        <env name="POSTMARK_INBOUND_SECRET" value="test-inbound-secret"/>
        <env name="ANTHROPIC_MODEL" value="claude-sonnet-5"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
